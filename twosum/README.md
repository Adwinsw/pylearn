# 总结
```
精简时间复杂度要点：如何能在一个循环内完成比较和取值两个过程

1、python3函数enumerate可以完成枚举和获取下角标的工作,得到形如((1,"1"),(2,"a"))这样的结构,再通过倒装输出到字典内,对于只需要单一解可使用此方法

2、在输出所有解的情况下,出现2个以上相同变量值可以符合条件,则无法避免双循环,形如([3,3,3],6)这样的输入条件,会出现[0,1],[0,2]这类解,或者可能还有我并不知道的算法可以完成O(N)
```